{
  "name": "@modux/editor",
  "version": "0.1.0",
  "description": "Graphical model editor for modux — Lit web component, embeddable in Mateu",
  "type": "module",
  "main": "dist/modux-editor.js",
  "module": "dist/modux-editor.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vitest run && vite build",
    "copy": "npm run build && mkdir -p ../model-driven-generator/src/main/resources/static/modux-editor && cp dist/*.js ../model-driven-generator/src/main/resources/static/modux-editor/ && (cp dist/*.js ../model-driven-generator/target/classes/static/modux-editor/ 2>/dev/null || true)",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "d3-selection": "^3.0.0",
    "d3-zoom": "^3.0.0",
    "elkjs": "^0.11.1",
    "lit": "^3.2.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/d3-selection": "^3.0.11",
    "@types/d3-zoom": "^3.0.8",
    "@types/node": "^22.20.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.7"
  }
}
