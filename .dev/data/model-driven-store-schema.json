{
  "$schema" : "https://json-schema.org/draft/2020-12/schema",
  "$defs" : {
    "AccessPolicyEntity" : {
      "type" : "object",
      "properties" : {
        "appliesToId" : {
          "type" : "string",
          "description" : "Reference to an existing 'appliesTo' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "description" : {
          "type" : "string"
        },
        "expression" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "AclEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "direction" : {
          "type" : "string"
        },
        "externalSystem" : {
          "type" : "string"
        },
        "gatewayId" : {
          "type" : "string",
          "description" : "Reference to an existing 'gateway' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "translatedDomainEventIds" : {
          "description" : "References to existing 'translatedDomainEvent' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "translatedUseCaseIds" : {
          "description" : "References to existing 'translatedUseCase' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        }
      },
      "additionalProperties" : false
    },
    "AggregateEntity" : {
      "type" : "object",
      "properties" : {
        "audited" : {
          "type" : "boolean"
        },
        "decisionIds" : {
          "description" : "References to existing 'decision' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "eventSourcingEnabled" : {
          "type" : "boolean"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "idType" : {
          "$ref" : "#/$defs/AggregateIdType"
        },
        "invariants" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/InvariantEntity"
          }
        },
        "lifecycle" : {
          "$ref" : "#/$defs/LifecycleEntity"
        },
        "modelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'model' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "operations" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/OperationEntity"
          }
        },
        "optimisticLockingEnabled" : {
          "type" : "boolean"
        },
        "persistenceType" : {
          "$ref" : "#/$defs/AggregatePersistenceType"
        },
        "snapshotFrequency" : {
          "type" : "integer"
        },
        "tableName" : {
          "type" : "string"
        },
        "tableSchema" : {
          "type" : "string"
        },
        "valueObjectIds" : {
          "description" : "References to existing 'valueObject' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "AggregateIdType" : {
      "type" : "string",
      "enum" : [ "UUID", "LONG", "STRING", "LOCATOR" ]
    },
    "AggregatePersistenceType" : {
      "type" : "string",
      "enum" : [ "JPA", "MONGODB", "EVENT_SOURCED", "IN_MEMORY" ]
    },
    "AiAgentEntity" : {
      "type" : "object",
      "properties" : {
        "allowedApiIds" : {
          "description" : "References to existing 'allowedApi' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "allowedApiOperationIds" : {
          "description" : "References to existing 'allowedApiOperation' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "allowedExternalUseCaseIds" : {
          "description" : "References to existing 'allowedExternalUseCase' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "allowedMcpServerIds" : {
          "description" : "References to existing 'allowedMcpServer' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "allowedQueryServiceIds" : {
          "description" : "References to existing 'allowedQueryService' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "allowedUseCaseIds" : {
          "description" : "References to existing 'allowedUseCase' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "delegateAgentIds" : {
          "description" : "References to existing 'delegateAgent' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "external" : {
          "type" : "boolean"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "mcpGatewayIds" : {
          "description" : "References to existing 'mcpGateway' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "name" : {
          "type" : "string"
        },
        "ragIds" : {
          "description" : "References to existing 'rag' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "reactsToEventIds" : {
          "description" : "References to existing 'reactsToEvent' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "AnonymizationStrategy" : {
      "type" : "string",
      "enum" : [ "NONE", "MASK", "HASH", "ERASE", "CRYPTO_SHRED" ]
    },
    "ApiEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "implementedByModuleIds" : {
          "description" : "References to existing 'implementedByModule' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "name" : {
          "type" : "string"
        },
        "operationImplementations" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ApiOperationImplementationEntity"
          }
        },
        "operations" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ApiOperationEntity"
          }
        },
        "publishedByExternalSystemId" : {
          "type" : "string",
          "description" : "Reference to an existing 'publishedByExternalSystem' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ApiOperationEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "httpMethod" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "path" : {
          "type" : "string"
        },
        "requestModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'requestModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "responseModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'responseModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "targetModuleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetModule' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "targetUseCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetUseCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "ApiOperationImplementationEntity" : {
      "type" : "object",
      "properties" : {
        "moduleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'module' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "operationId" : {
          "type" : "string",
          "description" : "Reference to an existing 'operation' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "useCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'useCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "ApplicationEventEntity" : {
      "type" : "object",
      "properties" : {
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "modelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'model' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "BddScenarioEntity" : {
      "type" : "object",
      "properties" : {
        "feature" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "steps" : {
          "type" : "string"
        },
        "tags" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "BffEntity" : {
      "type" : "object",
      "properties" : {
        "authRequired" : {
          "type" : "boolean"
        },
        "basePath" : {
          "type" : "string"
        },
        "clientType" : {
          "type" : "string"
        },
        "description" : {
          "type" : "string"
        },
        "exposedUseCaseIds" : {
          "description" : "References to existing 'exposedUseCase' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "BusinessRuleActionEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "domainEventId" : {
          "type" : "string",
          "description" : "Reference to an existing 'domainEvent' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "expression" : {
          "type" : "string"
        },
        "fieldId" : {
          "type" : "string",
          "description" : "Reference to an existing 'field' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "type" : {
          "type" : "string"
        },
        "useCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'useCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "BusinessRuleConditionEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "expression" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "BusinessRuleEntity" : {
      "type" : "object",
      "properties" : {
        "actions" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/BusinessRuleActionEntity"
          }
        },
        "conditions" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/BusinessRuleConditionEntity"
          }
        },
        "description" : {
          "type" : "string"
        },
        "enabled" : {
          "type" : "boolean"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "modelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'model' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "priority" : {
          "type" : "integer"
        },
        "ruleGroup" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "CacheProvider" : {
      "type" : "string",
      "enum" : [ "Redis", "Hazelcast", "Caffeine", "Custom" ]
    },
    "ComponentEntity" : {
      "type" : "object",
      "properties" : {
        "dataSourceType" : {
          "type" : "string"
        },
        "gatewayId" : {
          "type" : "string",
          "description" : "Reference to an existing 'gateway' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "presentationType" : {
          "type" : "string"
        },
        "queryServiceId" : {
          "type" : "string",
          "description" : "Reference to an existing 'queryService' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ContextMapRelationEntity" : {
      "type" : "object",
      "properties" : {
        "decisionIds" : {
          "description" : "References to existing 'decision' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "sourceModuleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'sourceModule' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "targetModuleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetModule' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "type" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "DbMigrationTool" : {
      "type" : "string",
      "enum" : [ "Flyway", "Liquibase", "None" ]
    },
    "DecisionEntity" : {
      "type" : "object",
      "properties" : {
        "decision" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "rationale" : {
          "type" : "string"
        },
        "source" : {
          "type" : "string"
        },
        "status" : {
          "$ref" : "#/$defs/DecisionStatus"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "DecisionStatus" : {
      "type" : "string",
      "enum" : [ "PROPOSED", "ACCEPTED", "DEPRECATED", "SUPERSEDED" ]
    },
    "DiagramEdgeEntity" : {
      "type" : "object",
      "properties" : {
        "points" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/DiagramPointEntity"
          }
        },
        "ref" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "DiagramEntity" : {
      "type" : "object",
      "properties" : {
        "collapsed" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "detail" : {
          "type" : "string"
        },
        "edges" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/DiagramEdgeEntity"
          }
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "nodes" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/DiagramNodeEntity"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "DiagramNodeEntity" : {
      "type" : "object",
      "properties" : {
        "h" : {
          "type" : "number"
        },
        "ref" : {
          "type" : "string"
        },
        "w" : {
          "type" : "number"
        },
        "x" : {
          "type" : "number"
        },
        "y" : {
          "type" : "number"
        }
      },
      "additionalProperties" : false
    },
    "DiagramPointEntity" : {
      "type" : "object",
      "properties" : {
        "x" : {
          "type" : "number"
        },
        "y" : {
          "type" : "number"
        }
      },
      "additionalProperties" : false
    },
    "DomainEventEntity" : {
      "type" : "object",
      "properties" : {
        "compressionType" : {
          "type" : "string"
        },
        "deadLetterQueueEnabled" : {
          "type" : "boolean"
        },
        "deadLetterQueueName" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "integrationModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'integrationModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "maxDeliveryAttempts" : {
          "type" : "integer"
        },
        "modelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'model' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "partitions" : {
          "type" : "integer"
        },
        "publishAsIntegrationEvent" : {
          "type" : "boolean"
        },
        "replayable" : {
          "type" : "boolean"
        },
        "retentionMs" : {
          "type" : "integer"
        },
        "routingKeyField" : {
          "type" : "string"
        },
        "schemaVersion" : {
          "type" : "string"
        },
        "serializationFormat" : {
          "type" : "string"
        },
        "topicName" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "DomainPolicyEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "triggeringEventId" : {
          "type" : "string",
          "description" : "Reference to an existing 'triggeringEvent' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "useCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'useCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "DomainServiceEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "operations" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/OperationEntity"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "EmailProvider" : {
      "type" : "string",
      "enum" : [ "SMTP", "SendGrid", "Mailgun", "AWSSes", "Custom" ]
    },
    "EntityEntity" : {
      "type" : "object",
      "properties" : {
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "isCollection" : {
          "type" : "boolean"
        },
        "modelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'model' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "parentAggregateId" : {
          "type" : "string",
          "description" : "Reference to an existing 'parentAggregate' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "EnumEntity" : {
      "type" : "object",
      "properties" : {
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "values" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/EnumValueEntity"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "EnumValueEntity" : {
      "type" : "object",
      "properties" : {
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "EnvVarEntity" : {
      "type" : "object",
      "properties" : {
        "defaultValue" : {
          "type" : "string"
        },
        "description" : {
          "type" : "string"
        },
        "name" : {
          "type" : "string"
        },
        "required" : {
          "type" : "boolean"
        },
        "secret" : {
          "type" : "boolean"
        }
      },
      "additionalProperties" : false
    },
    "ExternalApiOperationUseEntity" : {
      "type" : "object",
      "properties" : {
        "operationId" : {
          "type" : "string",
          "description" : "Reference to an existing 'operation' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "siteId" : {
          "type" : "string",
          "description" : "Reference to an existing 'site' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "ExternalSystemDirection" : {
      "type" : "string",
      "enum" : [ "INBOUND", "OUTBOUND", "BOTH" ]
    },
    "ExternalSystemEntity" : {
      "type" : "object",
      "properties" : {
        "apiOperationUses" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ExternalApiOperationUseEntity"
          }
        },
        "cqrsExternalSystemIds" : {
          "description" : "References to existing 'cqrsExternalSystem' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "decisionIds" : {
          "description" : "References to existing 'decision' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "dependsOnApiIds" : {
          "description" : "References to existing 'dependsOnApi' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "dependsOnExternalSystemIds" : {
          "description" : "References to existing 'dependsOnExternalSystem' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "direction" : {
          "$ref" : "#/$defs/ExternalSystemDirection"
        },
        "gatewayId" : {
          "type" : "string",
          "description" : "Reference to an existing 'gateway' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "mcpServers" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/McpServerEntity"
          }
        },
        "name" : {
          "type" : "string"
        },
        "owner" : {
          "type" : "string"
        },
        "protocol" : {
          "$ref" : "#/$defs/ExternalSystemProtocol"
        },
        "tables" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ExternalSystemTableEntity"
          }
        },
        "useCases" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ExternalSystemUseCaseEntity"
          }
        }
      },
      "additionalProperties" : false
    },
    "ExternalSystemProtocol" : {
      "type" : "string",
      "enum" : [ "REST", "SOAP", "GRPC", "MESSAGING", "FILE", "DATABASE", "OTHER" ]
    },
    "ExternalSystemTableEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "ExternalSystemUseCaseEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "FieldDataType" : {
      "type" : "string",
      "enum" : [ "integer", "string", "number", "date", "time", "dateTime", "bool", "array", "file", "status", "money", "component", "menu", "range", "action", "actionGroup", "dateRange" ]
    },
    "FileStorageProvider" : {
      "type" : "string",
      "enum" : [ "S3", "AzureBlob", "GCS", "Local", "Custom" ]
    },
    "FlowArchetype" : {
      "type" : "string",
      "enum" : [ "MATERIALIZES", "TRIGGERS", "NOTIFIES", "ORCHESTRATES" ]
    },
    "FlowEntity" : {
      "type" : "object",
      "properties" : {
        "archetype" : {
          "$ref" : "#/$defs/FlowArchetype"
        },
        "decisionIds" : {
          "description" : "References to existing 'decision' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "inputMappings" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "materializedFields" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "name" : {
          "type" : "string"
        },
        "overrides" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "readModelName" : {
          "type" : "string"
        },
        "targetModuleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetModule' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "targetUseCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetUseCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "triggerAggregateId" : {
          "type" : "string",
          "description" : "Reference to an existing 'triggerAggregate' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "triggerDomainServiceId" : {
          "type" : "string",
          "description" : "Reference to an existing 'triggerDomainService' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "triggerEvent" : {
          "type" : "string"
        },
        "triggerUseCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'triggerUseCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "GatewayAuthType" : {
      "type" : "string",
      "enum" : [ "None", "Basic", "ApiKey", "BearerToken", "OAuth2ClientCredentials" ]
    },
    "GatewayEntity" : {
      "type" : "object",
      "properties" : {
        "authApiKeyHeaderName" : {
          "type" : "string"
        },
        "authBearerToken" : {
          "type" : "string"
        },
        "authOAuth2ClientId" : {
          "type" : "string",
          "description" : "Reference to an existing 'authOAuth2Client' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "authOAuth2ClientSecret" : {
          "type" : "string"
        },
        "authOAuth2Scopes" : {
          "type" : "string"
        },
        "authOAuth2TokenUrl" : {
          "type" : "string"
        },
        "authPassword" : {
          "type" : "string"
        },
        "authType" : {
          "$ref" : "#/$defs/GatewayAuthType"
        },
        "authUsername" : {
          "type" : "string"
        },
        "baseUrl" : {
          "type" : "string"
        },
        "corsAllowedOrigins" : {
          "type" : "string"
        },
        "corsEnabled" : {
          "type" : "boolean"
        },
        "globalTimeoutMs" : {
          "type" : "integer"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "operations" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/GatewayOperationEntity"
          }
        },
        "rateLimitBurstSize" : {
          "type" : "integer"
        },
        "rateLimitEnabled" : {
          "type" : "boolean"
        },
        "rateLimitRequestsPerSecond" : {
          "type" : "integer"
        },
        "serviceId" : {
          "type" : "string",
          "description" : "Reference to an existing 'service' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "GatewayOperationEntity" : {
      "type" : "object",
      "properties" : {
        "circuitBreakerEnabled" : {
          "type" : "boolean"
        },
        "circuitBreakerFailureRateThreshold" : {
          "type" : "integer"
        },
        "circuitBreakerSlidingWindowSize" : {
          "type" : "integer"
        },
        "httpMethod" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "inputModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'inputModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "outputModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'outputModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "parameters" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/GatewayParameterEntity"
          }
        },
        "path" : {
          "type" : "string"
        },
        "retryMaxAttempts" : {
          "type" : "integer"
        },
        "retryWaitDurationMs" : {
          "type" : "integer"
        },
        "timeoutMs" : {
          "type" : "integer"
        }
      },
      "additionalProperties" : false
    },
    "GatewayParameterEntity" : {
      "type" : "object",
      "properties" : {
        "location" : {
          "type" : "string"
        },
        "name" : {
          "type" : "string"
        },
        "required" : {
          "type" : "boolean"
        },
        "type" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "IamProvider" : {
      "type" : "string",
      "enum" : [ "Keycloak", "Auth0", "Okta", "Cognito", "AzureAD", "Custom" ]
    },
    "IntegrationEventCompressionType" : {
      "type" : "string",
      "enum" : [ "NONE", "SNAPPY", "LZ4", "ZSTD" ]
    },
    "IntegrationEventEntity" : {
      "type" : "object",
      "properties" : {
        "compressionType" : {
          "$ref" : "#/$defs/IntegrationEventCompressionType"
        },
        "deadLetterQueueEnabled" : {
          "type" : "boolean"
        },
        "deadLetterQueueName" : {
          "type" : "string"
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "maxDeliveryAttempts" : {
          "type" : "integer"
        },
        "moduleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'module' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "partitions" : {
          "type" : "integer"
        },
        "payloadModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'payloadModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "replayable" : {
          "type" : "boolean"
        },
        "retentionMs" : {
          "type" : "integer"
        },
        "routingKeyField" : {
          "type" : "string"
        },
        "schemaVersion" : {
          "type" : "string"
        },
        "serializationFormat" : {
          "$ref" : "#/$defs/IntegrationEventSerializationFormat"
        },
        "sourceDomainEventId" : {
          "type" : "string",
          "description" : "Reference to an existing 'sourceDomainEvent' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "topicName" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "IntegrationEventSerializationFormat" : {
      "type" : "string",
      "enum" : [ "JSON", "AVRO", "PROTOBUF" ]
    },
    "InvariantConditionEntity" : {
      "type" : "object",
      "properties" : {
        "custom" : {
          "type" : "boolean"
        },
        "description" : {
          "type" : "string"
        },
        "errorMessage" : {
          "type" : "string"
        },
        "expression" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "InvariantEntity" : {
      "type" : "object",
      "properties" : {
        "conditions" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/InvariantConditionEntity"
          }
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "KpiEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "dimensionFields" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "measure" : {
          "$ref" : "#/$defs/KpiMeasure"
        },
        "name" : {
          "type" : "string"
        },
        "sourceDomainEventId" : {
          "type" : "string",
          "description" : "Reference to an existing 'sourceDomainEvent' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "timeGrain" : {
          "$ref" : "#/$defs/KpiTimeGrain"
        },
        "valueField" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "KpiMeasure" : {
      "type" : "string",
      "enum" : [ "COUNT", "SUM", "AVG", "MIN", "MAX" ]
    },
    "KpiTimeGrain" : {
      "type" : "string",
      "enum" : [ "HOUR", "DAY", "WEEK", "MONTH", "QUARTER", "YEAR" ]
    },
    "LifecycleEntity" : {
      "type" : "object",
      "properties" : {
        "initialState" : {
          "type" : "string"
        },
        "stateField" : {
          "type" : "string"
        },
        "states" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "transitions" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/LifecycleTransitionEntity"
          }
        }
      },
      "additionalProperties" : false
    },
    "LifecycleTransitionEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "fromState" : {
          "type" : "string"
        },
        "guard" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "operationId" : {
          "type" : "string",
          "description" : "Reference to an existing 'operation' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "toState" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "LlmProvider" : {
      "type" : "string",
      "enum" : [ "OpenAI", "Anthropic", "Google", "Azure", "Ollama", "Custom" ]
    },
    "LoggingProvider" : {
      "type" : "string",
      "enum" : [ "ELK", "Loki", "Splunk", "Datadog", "CloudWatch", "Custom" ]
    },
    "McpGatewayEntity" : {
      "type" : "object",
      "properties" : {
        "apiIds" : {
          "description" : "References to existing 'api' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "apiOperationIds" : {
          "description" : "References to existing 'apiOperation' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "mcpServerIds" : {
          "description" : "References to existing 'mcpServer' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "name" : {
          "type" : "string"
        },
        "ragIds" : {
          "description" : "References to existing 'rag' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "useCaseIds" : {
          "description" : "References to existing 'useCase' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "McpServerEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "uri" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "MessageBrokerType" : {
      "type" : "string",
      "enum" : [ "Kafka", "RabbitMQ", "ActiveMQ", "AWSSQS", "AzureServiceBus", "GooglePubSub", "Custom" ]
    },
    "MetricsProvider" : {
      "type" : "string",
      "enum" : [ "Prometheus", "Datadog", "NewRelic", "CloudWatch", "Custom" ]
    },
    "ModelEntity" : {
      "type" : "object",
      "properties" : {
        "fields" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ModelFieldEntity"
          }
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "validations" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ModelValidationEntity"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ModelFieldEntity" : {
      "type" : "object",
      "properties" : {
        "anonymizationStrategy" : {
          "$ref" : "#/$defs/AnonymizationStrategy"
        },
        "basicType" : {
          "type" : "boolean"
        },
        "enum" : {
          "type" : "boolean"
        },
        "enumId" : {
          "type" : "string",
          "description" : "Reference to an existing 'enum' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "modelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'model' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "piiClassification" : {
          "$ref" : "#/$defs/PiiClassification"
        },
        "type" : {
          "$ref" : "#/$defs/FieldDataType"
        },
        "validations" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ModelFieldValidationEntity"
          }
        }
      },
      "additionalProperties" : false
    },
    "ModelFieldValidationEntity" : {
      "type" : "object",
      "properties" : {
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "params" : {
          "type" : "string"
        },
        "type" : {
          "$ref" : "#/$defs/ModelFieldValidationType"
        }
      },
      "additionalProperties" : false
    },
    "ModelFieldValidationType" : {
      "type" : "string",
      "enum" : [ "NotNull", "NotEmpty", "NotBlank", "Size", "Min", "Max", "DecimalMin", "DecimalMax", "Pattern", "Email", "Positive", "PositiveOrZero", "Negative", "NegativeOrZero", "Future", "Past", "FutureOrPresent", "PastOrPresent", "Digits" ]
    },
    "ModelMappingEntity" : {
      "type" : "object",
      "properties" : {
        "hasCustomPart" : {
          "type" : "boolean"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "rules" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ModelMappingRuleEntity"
          }
        },
        "sourceModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'sourceModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "targetModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ModelMappingExpressionEntity" : {
      "type" : "object",
      "properties" : {
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "inputExpression" : {
          "type" : "string"
        },
        "outputExpression" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "ModelMappingRuleEntity" : {
      "type" : "object",
      "properties" : {
        "expressions" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ModelMappingExpressionEntity"
          }
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "sourceFieldId" : {
          "type" : "string",
          "description" : "Reference to an existing 'sourceField' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "targetFieldId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetField' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "ModelValidationEntity" : {
      "type" : "object",
      "properties" : {
        "condition" : {
          "type" : "string"
        },
        "fieldIds" : {
          "type" : "string",
          "description" : "References to existing 'field' ids (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "message" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "ModuleEntity" : {
      "type" : "object",
      "properties" : {
        "accessPolicies" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/AccessPolicyEntity"
          }
        },
        "acls" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/AclEntity"
          }
        },
        "aggregateIds" : {
          "description" : "References to existing 'aggregate' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "applicationEventIds" : {
          "description" : "References to existing 'applicationEvent' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "autoTableNamePrefix" : {
          "type" : "boolean"
        },
        "bddScenarios" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/BddScenarioEntity"
          }
        },
        "bffs" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/BffEntity"
          }
        },
        "decisionIds" : {
          "description" : "References to existing 'decision' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "domainEventIds" : {
          "description" : "References to existing 'domainEvent' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "domainPolicies" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/DomainPolicyEntity"
          }
        },
        "domainServiceIds" : {
          "description" : "References to existing 'domainService' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "entityIds" : {
          "description" : "References to existing 'entity' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "gitRepository" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "invariants" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/InvariantEntity"
          }
        },
        "kpis" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/KpiEntity"
          }
        },
        "llmSystemPrompt" : {
          "type" : "string"
        },
        "name" : {
          "type" : "string"
        },
        "projectionIds" : {
          "description" : "References to existing 'projection' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "readModelIds" : {
          "description" : "References to existing 'readModel' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "readSideExternalSystemId" : {
          "type" : "string",
          "description" : "Reference to an existing 'readSideExternalSystem' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "readSideModuleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'readSideModule' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "readSideVia" : {
          "type" : "string"
        },
        "sagaIds" : {
          "description" : "References to existing 'saga' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "scheduledTriggerIds" : {
          "description" : "References to existing 'scheduledTrigger' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "subdomainType" : {
          "$ref" : "#/$defs/SubdomainType"
        },
        "subscriptionIds" : {
          "description" : "References to existing 'subscription' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "tableNamePrefix" : {
          "type" : "string"
        },
        "useCaseIds" : {
          "description" : "References to existing 'useCase' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "valueObjectIds" : {
          "description" : "References to existing 'valueObject' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "version" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "OperationEntity" : {
      "type" : "object",
      "properties" : {
        "defaultPageSize" : {
          "type" : "integer"
        },
        "emits" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "inputModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'inputModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "intent" : {
          "type" : "string"
        },
        "name" : {
          "type" : "string"
        },
        "outputModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'outputModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "paginated" : {
          "type" : "boolean"
        },
        "preconditions" : {
          "type" : "string"
        },
        "sets" : {
          "type" : "string"
        },
        "type" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "PageButtonEntity" : {
      "type" : "object",
      "properties" : {
        "actionId" : {
          "type" : "string",
          "description" : "Reference to an existing 'action' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "icon" : {
          "type" : "string"
        },
        "label" : {
          "type" : "string"
        },
        "useCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'useCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "PageEntity" : {
      "type" : "object",
      "properties" : {
        "aggregateId" : {
          "type" : "string",
          "description" : "Reference to an existing 'aggregate' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "bottomBar" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/PageButtonEntity"
          }
        },
        "completionActions" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/PageButtonEntity"
          }
        },
        "componentIds" : {
          "description" : "References to existing 'component' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "fieldConfigs" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/PageFieldConfigEntity"
          }
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "listingDataSourceType" : {
          "type" : "string"
        },
        "listingGatewayId" : {
          "type" : "string",
          "description" : "Reference to an existing 'listingGateway' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "listingQueryServiceId" : {
          "type" : "string",
          "description" : "Reference to an existing 'listingQueryService' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "modelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'model' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "route" : {
          "type" : "string"
        },
        "rules" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/PageRuleEntity"
          }
        },
        "toolbar" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/PageButtonEntity"
          }
        },
        "triggers" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/PageTriggerEntity"
          }
        },
        "type" : {
          "type" : "string"
        },
        "validations" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/PageValidationEntity"
          }
        },
        "wizardSteps" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/PageWizardStepEntity"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "PageFieldConfigEntity" : {
      "type" : "object",
      "properties" : {
        "colspan" : {
          "type" : "integer"
        },
        "cssClass" : {
          "type" : "string"
        },
        "fieldId" : {
          "type" : "string",
          "description" : "Reference to an existing 'field' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "help" : {
          "type" : "string"
        },
        "label" : {
          "type" : "string"
        },
        "stereotype" : {
          "type" : "string"
        },
        "style" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "PageRuleEntity" : {
      "type" : "object",
      "properties" : {
        "action" : {
          "type" : "string"
        },
        "actionId" : {
          "type" : "string",
          "description" : "Reference to an existing 'action' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "expression" : {
          "type" : "string"
        },
        "fieldAttribute" : {
          "type" : "string"
        },
        "fieldName" : {
          "type" : "string"
        },
        "filter" : {
          "type" : "string"
        },
        "result" : {
          "type" : "string"
        },
        "value" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "PageTriggerEntity" : {
      "type" : "object",
      "properties" : {
        "actionId" : {
          "type" : "string",
          "description" : "Reference to an existing 'action' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "calledActionId" : {
          "type" : "string",
          "description" : "Reference to an existing 'calledAction' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "condition" : {
          "type" : "string"
        },
        "eventName" : {
          "type" : "string"
        },
        "propertyName" : {
          "type" : "string"
        },
        "timeoutMillis" : {
          "type" : "integer"
        },
        "times" : {
          "type" : "integer"
        },
        "type" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "PageValidationEntity" : {
      "type" : "object",
      "properties" : {
        "condition" : {
          "type" : "string"
        },
        "fieldId" : {
          "type" : "string",
          "description" : "Reference to an existing 'field' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "message" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "PageWizardStepEntity" : {
      "type" : "object",
      "properties" : {
        "label" : {
          "type" : "string"
        },
        "pageId" : {
          "type" : "string",
          "description" : "Reference to an existing 'page' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "PiiClassification" : {
      "type" : "string",
      "enum" : [ "NONE", "PII", "SENSITIVE" ]
    },
    "ProcessEntity" : {
      "type" : "object",
      "properties" : {
        "decisionIds" : {
          "description" : "References to existing 'decision' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "onCompletionEventName" : {
          "type" : "string"
        },
        "ownerModuleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'ownerModule' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "sla" : {
          "type" : "string"
        },
        "steps" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ProcessStepEntity"
          }
        },
        "triggerAggregateId" : {
          "type" : "string",
          "description" : "Reference to an existing 'triggerAggregate' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "triggerEvent" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ProcessStepEntity" : {
      "type" : "object",
      "properties" : {
        "compensationUseCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'compensationUseCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "deadline" : {
          "type" : "string"
        },
        "description" : {
          "type" : "string"
        },
        "escalationRoleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'escalationRole' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "roleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'role' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "type" : {
          "$ref" : "#/$defs/ProcessStepType"
        },
        "useCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'useCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "ProcessStepType" : {
      "type" : "string",
      "enum" : [ "AUTOMATED", "HUMAN" ]
    },
    "ProjectEntity" : {
      "type" : "object",
      "properties" : {
        "cacheProvider" : {
          "$ref" : "#/$defs/CacheProvider"
        },
        "cicdProvider" : {
          "type" : "string"
        },
        "contextMap" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ContextMapRelationEntity"
          }
        },
        "database" : {
          "type" : "string"
        },
        "dbMigrationTool" : {
          "$ref" : "#/$defs/DbMigrationTool"
        },
        "emailProvider" : {
          "$ref" : "#/$defs/EmailProvider"
        },
        "environments" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ProjectEnvironmentConfigEntity"
          }
        },
        "externalSystems" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ExternalSystemEntity"
          }
        },
        "fileStorageProvider" : {
          "$ref" : "#/$defs/FileStorageProvider"
        },
        "gitRepository" : {
          "type" : "string"
        },
        "iamProvider" : {
          "$ref" : "#/$defs/IamProvider"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "llmProvider" : {
          "$ref" : "#/$defs/LlmProvider"
        },
        "loggingProvider" : {
          "$ref" : "#/$defs/LoggingProvider"
        },
        "messageBrokerType" : {
          "$ref" : "#/$defs/MessageBrokerType"
        },
        "metricsProvider" : {
          "$ref" : "#/$defs/MetricsProvider"
        },
        "name" : {
          "type" : "string"
        },
        "objective" : {
          "type" : "string"
        },
        "outputPath" : {
          "type" : "string"
        },
        "packageName" : {
          "type" : "string"
        },
        "secretsProvider" : {
          "$ref" : "#/$defs/SecretsProvider"
        },
        "serviceIds" : {
          "description" : "References to existing 'service' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "tenancyStrategy" : {
          "$ref" : "#/$defs/TenancyStrategy"
        },
        "terraformBackendType" : {
          "$ref" : "#/$defs/TerraformBackendType"
        },
        "terraformProvider" : {
          "$ref" : "#/$defs/TerraformProvider"
        },
        "terraformProviderVersion" : {
          "type" : "string"
        },
        "tracingProvider" : {
          "$ref" : "#/$defs/TracingProvider"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ProjectEnvironmentConfigEntity" : {
      "type" : "object",
      "properties" : {
        "cachePassword" : {
          "type" : "string"
        },
        "cacheUrl" : {
          "type" : "string"
        },
        "cacheUsername" : {
          "type" : "string"
        },
        "emailFrom" : {
          "type" : "string"
        },
        "emailHost" : {
          "type" : "string"
        },
        "emailPassword" : {
          "type" : "string"
        },
        "emailPort" : {
          "type" : "integer"
        },
        "emailUsername" : {
          "type" : "string"
        },
        "environment" : {
          "type" : "string"
        },
        "fileStorageAccessKey" : {
          "type" : "string"
        },
        "fileStorageBucket" : {
          "type" : "string"
        },
        "fileStorageEndpoint" : {
          "type" : "string"
        },
        "fileStorageRegion" : {
          "type" : "string"
        },
        "fileStorageSecretKey" : {
          "type" : "string"
        },
        "iamAudience" : {
          "type" : "string"
        },
        "iamClientId" : {
          "type" : "string",
          "description" : "Reference to an existing 'iamClient' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "iamClientSecret" : {
          "type" : "string"
        },
        "iamRealm" : {
          "type" : "string"
        },
        "iamServerUrl" : {
          "type" : "string"
        },
        "ingressClassName" : {
          "type" : "string"
        },
        "ingressDomain" : {
          "type" : "string"
        },
        "ingressTlsEnabled" : {
          "type" : "boolean"
        },
        "kubernetesCertificateAuthorityData" : {
          "type" : "string"
        },
        "kubernetesClusterUrl" : {
          "type" : "string"
        },
        "kubernetesContext" : {
          "type" : "string"
        },
        "kubernetesNamespace" : {
          "type" : "string"
        },
        "kubernetesToken" : {
          "type" : "string"
        },
        "llmApiKey" : {
          "type" : "string"
        },
        "llmApiUrl" : {
          "type" : "string"
        },
        "llmModel" : {
          "type" : "string"
        },
        "loggingEndpoint" : {
          "type" : "string"
        },
        "messageBrokerPassword" : {
          "type" : "string"
        },
        "messageBrokerUrl" : {
          "type" : "string"
        },
        "messageBrokerUsername" : {
          "type" : "string"
        },
        "metricsEndpoint" : {
          "type" : "string"
        },
        "secretsEndpoint" : {
          "type" : "string"
        },
        "secretsToken" : {
          "type" : "string"
        },
        "terraformBackendBucket" : {
          "type" : "string"
        },
        "terraformBackendKey" : {
          "type" : "string"
        },
        "terraformBackendRegion" : {
          "type" : "string"
        },
        "terraformWorkspace" : {
          "type" : "string"
        },
        "tracingEndpoint" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "ProjectionEntity" : {
      "type" : "object",
      "properties" : {
        "errorHandlingStrategy" : {
          "type" : "string"
        },
        "handlers" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ProjectionEventHandlerEntity"
          }
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "maxRetries" : {
          "type" : "integer"
        },
        "name" : {
          "type" : "string"
        },
        "readModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'readModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "rebuildStrategy" : {
          "type" : "string"
        },
        "snapshotEnabled" : {
          "type" : "boolean"
        },
        "snapshotFrequency" : {
          "type" : "integer"
        },
        "sourceAggregateId" : {
          "type" : "string",
          "description" : "Reference to an existing 'sourceAggregate' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "sourceExternalTableId" : {
          "type" : "string",
          "description" : "Reference to an existing 'sourceExternalTable' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "sourceExternalUseCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'sourceExternalUseCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ProjectionEventHandlerEntity" : {
      "type" : "object",
      "properties" : {
        "domainEventId" : {
          "type" : "string",
          "description" : "Reference to an existing 'domainEvent' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "modelMappingId" : {
          "type" : "string",
          "description" : "Reference to an existing 'modelMapping' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "type" : {
          "$ref" : "#/$defs/ProjectionEventHandlerType"
        }
      },
      "additionalProperties" : false
    },
    "ProjectionEventHandlerType" : {
      "type" : "string",
      "enum" : [ "Create", "Update", "Delete", "Upsert", "Custom" ]
    },
    "ProxyApiEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "operationRoutes" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/ProxyOperationRouteEntity"
          }
        },
        "publishedByExternalSystemId" : {
          "type" : "string",
          "description" : "Reference to an existing 'publishedByExternalSystem' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "targetApiId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetApi' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ProxyOperationRouteEntity" : {
      "type" : "object",
      "properties" : {
        "operationId" : {
          "type" : "string",
          "description" : "Reference to an existing 'operation' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "targetSiteId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetSite' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "QueryCardinality" : {
      "type" : "string",
      "enum" : [ "Single", "List", "Page" ]
    },
    "QueryOperationEntity" : {
      "type" : "object",
      "properties" : {
        "cardinality" : {
          "$ref" : "#/$defs/QueryCardinality"
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "inputModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'inputModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "outputModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'outputModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "QueryServiceEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "exposedAsGrpc" : {
          "type" : "boolean"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "moduleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'module' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "operations" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/QueryOperationEntity"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "RagContentSourceEntity" : {
      "type" : "object",
      "properties" : {
        "type" : {
          "type" : "string"
        },
        "uri" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "RagEntity" : {
      "type" : "object",
      "properties" : {
        "contentSources" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/RagContentSourceEntity"
          }
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "sourceApiIds" : {
          "description" : "References to existing 'sourceApi' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "sourceExternalSystemIds" : {
          "description" : "References to existing 'sourceExternalSystem' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "sourceExternalTableIds" : {
          "description" : "References to existing 'sourceExternalTable' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "sourceModuleIds" : {
          "description" : "References to existing 'sourceModule' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "sourceReadModelIds" : {
          "description" : "References to existing 'sourceReadModel' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ReadModelConsistency" : {
      "type" : "string",
      "enum" : [ "Eventual", "Strong" ]
    },
    "ReadModelEntity" : {
      "type" : "object",
      "properties" : {
        "aggregateId" : {
          "type" : "string",
          "description" : "Reference to an existing 'aggregate' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "consistency" : {
          "$ref" : "#/$defs/ReadModelConsistency"
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "modelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'model' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "moduleId" : {
          "type" : "string",
          "description" : "Reference to an existing 'module' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "storageType" : {
          "$ref" : "#/$defs/ReadModelStorageType"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ReadModelStorageType" : {
      "type" : "string",
      "enum" : [ "Document", "Relational", "KeyValue", "InMemory" ]
    },
    "RoleEntity" : {
      "type" : "object",
      "properties" : {
        "aiAgentIds" : {
          "description" : "References to existing 'aiAgent' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "allowedQueryServiceIds" : {
          "description" : "References to existing 'allowedQueryService' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "allowedUseCaseIds" : {
          "description" : "References to existing 'allowedUseCase' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "externalSystemIds" : {
          "description" : "References to existing 'externalSystem' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "SagaEntity" : {
      "type" : "object",
      "properties" : {
        "compensationTimeoutMs" : {
          "type" : "integer"
        },
        "deadLetterQueue" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "maxRetries" : {
          "type" : "integer"
        },
        "name" : {
          "type" : "string"
        },
        "persistenceEnabled" : {
          "type" : "boolean"
        },
        "retryBackoffMs" : {
          "type" : "integer"
        },
        "steps" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/SagaStepEntity"
          }
        },
        "timeoutMs" : {
          "type" : "integer"
        },
        "triggeringEventIds" : {
          "description" : "References to existing 'triggeringEvent' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "SagaStepEntity" : {
      "type" : "object",
      "properties" : {
        "aggregateId" : {
          "type" : "string",
          "description" : "Reference to an existing 'aggregate' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "compensatingStepId" : {
          "type" : "string",
          "description" : "Reference to an existing 'compensatingStep' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "domainEventId" : {
          "type" : "string",
          "description" : "Reference to an existing 'domainEvent' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "gatewayId" : {
          "type" : "string",
          "description" : "Reference to an existing 'gateway' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "gatewayOperationId" : {
          "type" : "string",
          "description" : "Reference to an existing 'gatewayOperation' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "modelMappingId" : {
          "type" : "string",
          "description" : "Reference to an existing 'modelMapping' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "operationId" : {
          "type" : "string",
          "description" : "Reference to an existing 'operation' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "type" : {
          "$ref" : "#/$defs/SagaStepType"
        },
        "useCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'useCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "SagaStepType" : {
      "type" : "string",
      "enum" : [ "Custom", "ReadAggregate", "CallAggregateOperation", "SaveAggregate", "CallGateway", "PublishDomainEvent", "CallUseCase", "ApplyModelMapping" ]
    },
    "ScheduledTriggerEntity" : {
      "type" : "object",
      "properties" : {
        "allowConcurrentExecution" : {
          "type" : "boolean"
        },
        "cronExpression" : {
          "type" : "string"
        },
        "description" : {
          "type" : "string"
        },
        "executionEnvironment" : {
          "type" : "string"
        },
        "failureNotificationEmail" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "lockProvider" : {
          "type" : "string"
        },
        "maxExecutionTimeMs" : {
          "type" : "integer"
        },
        "misfirePolicy" : {
          "type" : "string"
        },
        "modelMappingId" : {
          "type" : "string",
          "description" : "Reference to an existing 'modelMapping' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "retryCount" : {
          "type" : "integer"
        },
        "retryOnFailure" : {
          "type" : "boolean"
        },
        "timezone" : {
          "type" : "string"
        },
        "useCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'useCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "SecretsProvider" : {
      "type" : "string",
      "enum" : [ "Vault", "AWSSecretsManager", "AzureKeyVault", "GCPSecretManager", "Custom" ]
    },
    "ServiceEntity" : {
      "type" : "object",
      "properties" : {
        "circuitBreakerEnabled" : {
          "type" : "boolean"
        },
        "circuitBreakerThreshold" : {
          "type" : "integer"
        },
        "connectionTimeoutMs" : {
          "type" : "integer"
        },
        "contextPath" : {
          "type" : "string"
        },
        "database" : {
          "type" : "string"
        },
        "dbMigrationTool" : {
          "$ref" : "#/$defs/DbMigrationTool"
        },
        "deploymentStrategy" : {
          "type" : "string"
        },
        "dockerImageName" : {
          "type" : "string"
        },
        "dockerImageRegistry" : {
          "type" : "string"
        },
        "envVars" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/EnvVarEntity"
          }
        },
        "gatewayIds" : {
          "description" : "References to existing 'gateway' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "gitRepository" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "javaVersion" : {
          "type" : "string"
        },
        "kubernetesCpuLimit" : {
          "type" : "string"
        },
        "kubernetesCpuRequest" : {
          "type" : "string"
        },
        "kubernetesHpaCpuThreshold" : {
          "type" : "integer"
        },
        "kubernetesHpaEnabled" : {
          "type" : "boolean"
        },
        "kubernetesHpaMaxReplicas" : {
          "type" : "integer"
        },
        "kubernetesHpaMinReplicas" : {
          "type" : "integer"
        },
        "kubernetesMemoryLimit" : {
          "type" : "string"
        },
        "kubernetesMemoryRequest" : {
          "type" : "string"
        },
        "kubernetesReplicas" : {
          "type" : "integer"
        },
        "livenessProbe" : {
          "type" : "string"
        },
        "moduleIds" : {
          "description" : "References to existing 'module' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "name" : {
          "type" : "string"
        },
        "openApiDocumentationEnabled" : {
          "type" : "boolean"
        },
        "outboxEnabled" : {
          "type" : "boolean"
        },
        "outboxTableName" : {
          "type" : "string"
        },
        "owner" : {
          "type" : "string"
        },
        "port" : {
          "type" : "integer"
        },
        "readTimeoutMs" : {
          "type" : "integer"
        },
        "readinessProbe" : {
          "type" : "string"
        },
        "startupProbe" : {
          "type" : "string"
        },
        "writeTimeoutMs" : {
          "type" : "integer"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "SolutionEntity" : {
      "type" : "object",
      "properties" : {
        "decisionIds" : {
          "description" : "References to existing 'decision' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "status" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "SubdomainType" : {
      "type" : "string",
      "enum" : [ "CORE", "SUPPORTING", "GENERIC" ]
    },
    "SubscriptionActionEntity" : {
      "type" : "object",
      "properties" : {
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "modelMappingId" : {
          "type" : "string",
          "description" : "Reference to an existing 'modelMapping' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "projectionId" : {
          "type" : "string",
          "description" : "Reference to an existing 'projection' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "sagaId" : {
          "type" : "string",
          "description" : "Reference to an existing 'saga' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "type" : {
          "$ref" : "#/$defs/SubscriptionActionType"
        },
        "useCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'useCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "SubscriptionActionType" : {
      "type" : "string",
      "enum" : [ "CallUseCase", "StartSaga", "UpdateProjection", "Custom" ]
    },
    "SubscriptionEntity" : {
      "type" : "object",
      "properties" : {
        "actions" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/SubscriptionActionEntity"
          }
        },
        "batchSize" : {
          "type" : "integer"
        },
        "batchTimeout" : {
          "type" : "integer"
        },
        "consumerGroup" : {
          "type" : "string"
        },
        "consumerTimeout" : {
          "type" : "integer"
        },
        "deadLetterTopic" : {
          "type" : "string"
        },
        "eventName" : {
          "type" : "string"
        },
        "filterExpression" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "idempotencyEnabled" : {
          "type" : "boolean"
        },
        "idempotencyKeyField" : {
          "type" : "string"
        },
        "inputModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'inputModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "offsetResetStrategy" : {
          "type" : "string"
        },
        "retryCount" : {
          "type" : "integer"
        },
        "scalingStrategy" : {
          "type" : "string"
        },
        "sourceService" : {
          "type" : "string"
        },
        "topicName" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "TenancyStrategy" : {
      "type" : "string",
      "enum" : [ "NONE", "SHARED_SCHEMA", "SCHEMA_PER_TENANT", "DATABASE_PER_TENANT" ]
    },
    "TerraformBackendType" : {
      "type" : "string",
      "enum" : [ "Local", "S3", "AzureRM", "GCS", "Consul", "HTTP" ]
    },
    "TerraformProvider" : {
      "type" : "string",
      "enum" : [ "AWS", "Azure", "GCP", "DigitalOcean", "Custom" ]
    },
    "TracingProvider" : {
      "type" : "string",
      "enum" : [ "Jaeger", "Zipkin", "OpenTelemetry", "Datadog", "AWSXRay", "Custom" ]
    },
    "UiAdapterEntity" : {
      "type" : "object",
      "properties" : {
        "appVariant" : {
          "$ref" : "#/$defs/UiAppVariant"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "menuItems" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/UiMenuItemEntity"
          }
        },
        "name" : {
          "type" : "string"
        },
        "path" : {
          "type" : "string"
        },
        "serviceId" : {
          "type" : "string",
          "description" : "Reference to an existing 'service' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "title" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "UiAppVariant" : {
      "type" : "string",
      "enum" : [ "HAMBURGUER_MENU", "MENU_ON_LEFT", "MENU_ON_TOP", "TABS", "AUTO" ]
    },
    "UiMenuItemEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "icon" : {
          "type" : "string"
        },
        "label" : {
          "type" : "string"
        },
        "route" : {
          "type" : "string"
        }
      },
      "additionalProperties" : false
    },
    "UiShellEntity" : {
      "type" : "object",
      "properties" : {
        "appVariant" : {
          "type" : "string"
        },
        "bucketName" : {
          "type" : "string"
        },
        "bucketProvider" : {
          "type" : "string"
        },
        "bucketRegion" : {
          "type" : "string"
        },
        "cdnProvider" : {
          "type" : "string"
        },
        "cdnSiteId" : {
          "type" : "string",
          "description" : "Reference to an existing 'cdnSite' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "deploymentServiceId" : {
          "type" : "string",
          "description" : "Reference to an existing 'deploymentService' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "deploymentType" : {
          "type" : "string"
        },
        "designSystem" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "serviceIds" : {
          "description" : "References to existing 'service' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "title" : {
          "type" : "string"
        },
        "url" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "UseCaseEntity" : {
      "type" : "object",
      "properties" : {
        "allowedRoles" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "allowedScopes" : {
          "type" : "array",
          "items" : {
            "type" : "string"
          }
        },
        "apiVersion" : {
          "type" : "string"
        },
        "asyncConsumerGroup" : {
          "type" : "string"
        },
        "asyncDeadLetterQueue" : {
          "type" : "string"
        },
        "asyncOrderingKey" : {
          "type" : "string"
        },
        "asyncRetryCount" : {
          "type" : "integer"
        },
        "asyncTopicName" : {
          "type" : "string"
        },
        "cacheTtlSeconds" : {
          "type" : "integer"
        },
        "cacheable" : {
          "type" : "boolean"
        },
        "decisionIds" : {
          "description" : "References to existing 'decision' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "exposedAsAsync" : {
          "type" : "boolean"
        },
        "exposedAsGrpc" : {
          "type" : "boolean"
        },
        "exposedAsMcp" : {
          "type" : "boolean"
        },
        "exposedAsRest" : {
          "type" : "boolean"
        },
        "exposedAsUi" : {
          "type" : "boolean"
        },
        "grpcMethodName" : {
          "type" : "string"
        },
        "grpcServiceName" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "idempotencyEnabled" : {
          "type" : "boolean"
        },
        "idempotencyKeyField" : {
          "type" : "string"
        },
        "inputModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'inputModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "mcpDescription" : {
          "type" : "string"
        },
        "name" : {
          "type" : "string"
        },
        "outputModelId" : {
          "type" : "string",
          "description" : "Reference to an existing 'outputModel' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "policy" : {
          "type" : "boolean"
        },
        "rateLimitEnabled" : {
          "type" : "boolean"
        },
        "rateLimitRequestsPerSecond" : {
          "type" : "integer"
        },
        "restHttpMethod" : {
          "type" : "string"
        },
        "restPath" : {
          "type" : "string"
        },
        "steps" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/UseCaseStepEntity"
          }
        },
        "timeoutMs" : {
          "type" : "integer"
        },
        "transactionBoundary" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "UseCaseStepEntity" : {
      "type" : "object",
      "properties" : {
        "aggregateId" : {
          "type" : "string",
          "description" : "Reference to an existing 'aggregate' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "applicationEventId" : {
          "type" : "string",
          "description" : "Reference to an existing 'applicationEvent' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "domainEventId" : {
          "type" : "string",
          "description" : "Reference to an existing 'domainEvent' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "externalUseCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'externalUseCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "gatewayId" : {
          "type" : "string",
          "description" : "Reference to an existing 'gateway' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "gatewayOperationId" : {
          "type" : "string",
          "description" : "Reference to an existing 'gatewayOperation' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "intent" : {
          "type" : "string"
        },
        "modelMappingId" : {
          "type" : "string",
          "description" : "Reference to an existing 'modelMapping' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "operationId" : {
          "type" : "string",
          "description" : "Reference to an existing 'operation' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "queryOperationId" : {
          "type" : "string",
          "description" : "Reference to an existing 'queryOperation' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "queryServiceId" : {
          "type" : "string",
          "description" : "Reference to an existing 'queryService' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "type" : {
          "$ref" : "#/$defs/UseCaseStepType"
        },
        "useCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'useCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    },
    "UseCaseStepType" : {
      "type" : "string",
      "enum" : [ "Custom", "ReadAggregate", "CallAggregateOperation", "SaveAggregate", "CallGateway", "CallExternalUseCase", "PublishDomainEvent", "PublishApplicationEvent", "CallUseCase", "CallQueryService", "ApplyModelMapping" ]
    },
    "ValueObjectEntity" : {
      "type" : "object",
      "properties" : {
        "dataType" : {
          "type" : "string"
        },
        "fieldsJson" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "type" : {
          "type" : "string"
        },
        "valuesJson" : {
          "type" : "string"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "ViewEntity" : {
      "type" : "object",
      "properties" : {
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "kind" : {
          "type" : "string"
        },
        "memberIds" : {
          "description" : "References to existing 'member' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "name" : {
          "type" : "string"
        },
        "seedId" : {
          "type" : "string",
          "description" : "Reference to an existing 'seed' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "WorkflowEntity" : {
      "type" : "object",
      "properties" : {
        "decisionIds" : {
          "description" : "References to existing 'decision' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "onCompletionEventName" : {
          "type" : "string"
        },
        "steps" : {
          "type" : "array",
          "items" : {
            "$ref" : "#/$defs/WorkflowStepEntity"
          }
        },
        "triggerAggregateId" : {
          "type" : "string",
          "description" : "Reference to an existing 'triggerAggregate' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "triggerDomainServiceId" : {
          "type" : "string",
          "description" : "Reference to an existing 'triggerDomainService' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "triggerEvent" : {
          "type" : "string"
        },
        "triggerUseCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'triggerUseCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "required" : [ "id" ],
      "additionalProperties" : false
    },
    "WorkflowStepEntity" : {
      "type" : "object",
      "properties" : {
        "completionEventName" : {
          "type" : "string"
        },
        "dependsOnStepIds" : {
          "description" : "References to existing 'dependsOnStep' ids (referential integrity is checked by the linter).",
          "type" : "array",
          "items" : {
            "type" : "string",
            "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
          }
        },
        "description" : {
          "type" : "string"
        },
        "emittedEventName" : {
          "type" : "string"
        },
        "id" : {
          "type" : "string",
          "description" : "Unique id of this element in the model; other elements reference it via *Id fields. No spaces (kebab-case recommended).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        },
        "name" : {
          "type" : "string"
        },
        "targetUseCaseId" : {
          "type" : "string",
          "description" : "Reference to an existing 'targetUseCase' id (referential integrity is checked by the linter).",
          "pattern" : "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        }
      },
      "additionalProperties" : false
    }
  },
  "type" : "object",
  "properties" : {
    "aggregates" : {
      "description" : "DDD aggregate roots: operations, invariants, lifecycle, persistence and event sourcing.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/AggregateEntity"
      }
    },
    "aiAgents" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/AiAgentEntity"
      }
    },
    "apis" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ApiEntity"
      }
    },
    "applicationEvents" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ApplicationEventEntity"
      }
    },
    "bddScenarios" : {
      "description" : "Given/when/then scenarios attached to use cases.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/BddScenarioEntity"
      }
    },
    "businessRules" : {
      "description" : "Declarative condition/action rules evaluated by the runtime.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/BusinessRuleEntity"
      }
    },
    "components" : {
      "description" : "Reusable UI components.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ComponentEntity"
      }
    },
    "decisions" : {
      "description" : "Architecture decision records (ADRs); elements trace to them via decisionIds.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/DecisionEntity"
      }
    },
    "diagrams" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/DiagramEntity"
      }
    },
    "domainEvents" : {
      "description" : "Events emitted by aggregates inside the bounded context.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/DomainEventEntity"
      }
    },
    "domainServices" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/DomainServiceEntity"
      }
    },
    "entities" : {
      "description" : "Domain entities that live inside an aggregate boundary.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/EntityEntity"
      }
    },
    "enums" : {
      "description" : "Enumerated types shared by models.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/EnumEntity"
      }
    },
    "flows" : {
      "description" : "Intent layer: one line of intent (materializes/triggers/notifies/orchestrates) expanded into structure.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/FlowEntity"
      }
    },
    "gateways" : {
      "description" : "Outbound ports to external services, with circuit breakers and rate limits.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/GatewayEntity"
      }
    },
    "integrationEvents" : {
      "description" : "Events published across context boundaries (with DLQ/versioning concerns).",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/IntegrationEventEntity"
      }
    },
    "invariants" : {
      "description" : "Reusable invariant definitions referenced by aggregates.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/InvariantEntity"
      }
    },
    "mcpGateways" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/McpGatewayEntity"
      }
    },
    "modelMappings" : {
      "description" : "Field-level mappings between models (e.g. DTO ↔ domain).",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ModelMappingEntity"
      }
    },
    "models" : {
      "description" : "Data shapes (fields + validations) backing aggregates, DTOs and read models.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ModelEntity"
      }
    },
    "modules" : {
      "description" : "Bounded contexts: the unit that owns aggregates, use cases and the published language.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ModuleEntity"
      }
    },
    "pages" : {
      "description" : "UI pages (CRUD, wizards) generated for the application.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/PageEntity"
      }
    },
    "processes" : {
      "description" : "Intent layer: human/automated step lists expanded into sagas, worklists and deadlines.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ProcessEntity"
      }
    },
    "projections" : {
      "description" : "Event handlers that maintain denormalized read models.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ProjectionEntity"
      }
    },
    "projects" : {
      "description" : "Deliverable systems: output path, package, services, tenancy, external systems, context map.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ProjectEntity"
      }
    },
    "proxyApis" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ProxyApiEntity"
      }
    },
    "queryServices" : {
      "description" : "Read-side services exposing query operations.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/QueryServiceEntity"
      }
    },
    "rags" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/RagEntity"
      }
    },
    "readModels" : {
      "description" : "Denormalized views kept by projections.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ReadModelEntity"
      }
    },
    "roles" : {
      "description" : "RBAC role definitions.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/RoleEntity"
      }
    },
    "sagas" : {
      "description" : "Multi-step workflows with compensation (orchestration or choreography).",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/SagaEntity"
      }
    },
    "scheduledTriggers" : {
      "description" : "Cron-based tasks that invoke use cases.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ScheduledTriggerEntity"
      }
    },
    "services" : {
      "description" : "Microservices of a project; each owns modules (bounded contexts).",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ServiceEntity"
      }
    },
    "solutions" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/SolutionEntity"
      }
    },
    "subscriptions" : {
      "description" : "Message handlers with filtering, retry and DLQ policies.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/SubscriptionEntity"
      }
    },
    "uiAdapters" : {
      "description" : "Bindings between pages and use cases.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/UiAdapterEntity"
      }
    },
    "uiShells" : {
      "description" : "Application shells: menus aggregating pages across modules.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/UiShellEntity"
      }
    },
    "useCases" : {
      "description" : "Application services: the commands/queries the system exposes.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/UseCaseEntity"
      }
    },
    "valueObjects" : {
      "description" : "Immutable typed wrappers (e.g. BookingId, Email) used by models and aggregates.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ValueObjectEntity"
      }
    },
    "views" : {
      "description" : "Named slices of the model for focused work and partial generation.",
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/ViewEntity"
      }
    },
    "workflows" : {
      "type" : "array",
      "items" : {
        "$ref" : "#/$defs/WorkflowEntity"
      }
    }
  },
  "additionalProperties" : false
}