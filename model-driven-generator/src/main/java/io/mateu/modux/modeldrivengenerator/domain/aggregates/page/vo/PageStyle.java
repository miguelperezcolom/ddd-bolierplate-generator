package io.mateu.modux.modeldrivengenerator.domain.aggregates.page.vo;

/**
 * The page-level style preset — one of mateu's {@code StyleConstants}: the
 * generated page carries the constant of the same name in its {@code @Style}.
 */
public enum PageStyle {
    CONTAINER,
    FULL_WIDTH,
    FULL_WIDTH_WITH_PADDING,
    COMPACT
}
